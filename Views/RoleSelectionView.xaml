<UserControl x:Class="PROG6212_WPF.Views.RoleSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
        <TextBlock Text="Select Your Role" FontSize="20" Margin="0,0,0,20" HorizontalAlignment="Center" />
        <Button Content="Lecturer" Width="200" Margin="10" Command="{Binding RoleSelectedCommand}" CommandParameter="Lecturer" />
        <Button Content="Academic Manager" Width="200" Margin="10" Command="{Binding RoleSelectedCommand}" CommandParameter="AcademicManager" />
        <Button Content="HR" Width="200" Margin="10" Command="{Binding RoleSelectedCommand}" CommandParameter="HR" />
    </StackPanel>
</UserControl>

